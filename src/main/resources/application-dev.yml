server:
  port: 5000

spring:
  application:
    name: roim-olam-server

  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
    properties.hibernate.dialect: org.hibernate.dialect.MySQL5InnoDBDialect

  datasource:
    platform: mysql
    url: jdbc:mysql://localhost:3306/roim_olam
    username: tav
    password: 1234
    driverClassName: com.mysql.cj.jdbc.Driver