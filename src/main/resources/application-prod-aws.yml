server:
  port: 5000

spring:
  application:
    name: roim-olam-server

  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
    properties.hibernate.dialect: org.hibernate.dialect.MySQL5InnoDBDialect

  datasource:
    platform: mysql
    url: jdbc:postgresql://roim-olam-msql.cbxt9i4cijlc.eu-central-1.rds.amazonaws.com:3306/roim_olam
    username: root
    password: 12345678
    driverClassName: com.mysql.cj.jdbc.Driver